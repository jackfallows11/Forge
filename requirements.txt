# Forge - Modular Training Engine for Diffusion Transformers
# Install with: pip install -r requirements.txt

# Core ML
torch>=2.4.0
torchvision>=0.19.0

# Hugging Face — pin these together to avoid version conflicts
# Dinov2WithRegistersConfig (needed by diffusers) was added in transformers 4.47.0
diffusers>=0.32.0
transformers>=4.47.0
accelerate>=0.33.0

# Model weights format
safetensors>=0.4.3

# Hugging Face Hub (for from_pretrained downloads)
huggingface_hub>=0.24.0

# Image handling (needed by diffusers pipelines)
Pillow>=10.0.0

# Utilities
numpy>=1.26.0
tqdm>=4.66.0